<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base de la aplicación. Es el padre del tema de la Splash Screen -->
    <style name="Theme.FitMacros" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema para la Splash Screen -->
    <style name="Theme.FitMacros.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand_blue</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.FitMacros</item>
    </style>
</resources>
